import { EnlargeChevronDirective } from './enlarge-chevron.directive';

describe('EnlargeChevronDirective', () => {
  it('should create an instance', () => {
    const directive = new EnlargeChevronDirective();
    expect(directive).toBeTruthy();
  });
});
